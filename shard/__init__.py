"""shard — c0mpute's decentralized inference engine.

one model, split across a swarm of consumer gpus, served over the open internet.
see docs/ARCHITECTURE.md. nothing here serves yet; these are the interface stubs
the build fills in, phase by phase (docs/ROADMAP.md).
"""

__version__ = "0.0.0"
